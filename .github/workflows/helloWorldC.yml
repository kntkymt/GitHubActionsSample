name: helloWorldC

on:
  push:
    branches:
      - 'c/**'

jobs:
  build:
    runs-on: [ubuntu-latest, macOS-latest]

    steps:
    - uses: actions/checkout@v2
    - name: helloWorld
      run: |
        gcc -o helloWorld main.c
        ./helloWorld